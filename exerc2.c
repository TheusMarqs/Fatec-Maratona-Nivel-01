#include <stdio.h>

int main()
{
    int A, B, X;

    // Leitura dos valores de A e B
    scanf("%d", &A);
    scanf("%d", &B);

    // Soma de A e B
    X = A + B;

    // Impressão do resultado
    printf("X = %d\n", X);

    return 0;
}